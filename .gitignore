# --- Build outputs ---
build/
out/
bin/
.gradle/

# --- Gradle wrapper is committed (gradlew, gradlew.bat, gradle-wrapper.jar,
# gradle-wrapper.properties) so the project builds from a clean clone with no
# system Gradle installed. Do not ignore it. ---

# --- JVM crash logs ---
hs_err_pid*
replay_pid*
*.log

# --- IDE ---
.idea/
*.iml
*.ipr
*.iws
.vscode/
.classpath
.project
.settings/
.metadata/
.recommenders/

# --- OS cruft ---
.DS_Store
Thumbs.db
desktop.ini

# --- Compiled classes (defensive; build/ above catches most) ---
*.class
target/